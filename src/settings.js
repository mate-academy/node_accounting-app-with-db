'use strict';

const PORT = 3000;

const DATABASE_CONFIG = {
  database: 'postgres',
  username: 'postgres',
  password: 'pass',
  host: 'localhost',
};

module.exports = {
  DATABASE_CONFIG, PORT,
};
